services:
  redis:
    image: redis:latest
    container_name: bv_redis
    restart: always
    volumes:
      - redis_data:/data
    networks:
      - bv_network

networks:
  bv_network:
    external: true

volumes:
  redis_data:
